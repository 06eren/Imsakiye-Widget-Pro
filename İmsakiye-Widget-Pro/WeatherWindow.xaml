<Window x:Class="İmsakiye_Widget_Pro.WeatherWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hava Durumu" Height="400" Width="500"
        WindowStartupLocation="CenterScreen"
        Background="#1e272e">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Border Grid.Row="0" Background="#0984e3" Padding="15">
            <TextBlock Text="🌤️ HAVA DURUMU" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
        </Border>
        
        <StackPanel Grid.Row="1" Margin="30" VerticalAlignment="Center">
            <!-- Şehir -->
            <TextBlock x:Name="CityText" Text="Ankara" FontSize="28" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            
            <!-- Ana Bilgi -->
            <Border Background="#2d3436" CornerRadius="15" Padding="30" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock x:Name="TempText" Text="25°C" FontSize="64" FontWeight="Bold" Foreground="#0984e3" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="DescText" Text="Açık" FontSize="20" Foreground="White" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
            
            <!-- Detaylar -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Border Grid.Column="0" Background="#2d3436" CornerRadius="10" Padding="15" Margin="0,0,5,0">
                    <StackPanel>
                        <TextBlock Text="💧 Nem" FontSize="14" Foreground="#b2bec3" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="HumidityText" Text="60%" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
                
                <Border Grid.Column="1" Background="#2d3436" CornerRadius="10" Padding="15" Margin="5,0,0,0">
                    <StackPanel>
                        <TextBlock Text="💨 Rüzgar" FontSize="14" Foreground="#b2bec3" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="WindText" Text="15 km/h" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>
            
            <Button Content="🔄 Yenile" Click="Refresh_Click" Background="#0984e3" Foreground="White" BorderThickness="0" Padding="20,10" Margin="0,20,0,0" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>
