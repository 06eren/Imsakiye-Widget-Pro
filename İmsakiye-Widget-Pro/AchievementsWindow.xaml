<Window x:Class="İmsakiye_Widget_Pro.AchievementsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rozetler ve Başarılar" Height="650" Width="800"
        WindowStartupLocation="CenterScreen"
        Background="#1e272e">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Border Grid.Row="0" Background="#ff7675" Padding="15">
            <TextBlock Text="🏆 ROZETLER VE BAŞARILAR" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
        </Border>
        
        <Border Grid.Row="1" Background="#2d3436" Padding="20" Margin="30,30,30,20">
            <StackPanel>
                <TextBlock Text="Seviye 12 - Mümin" FontSize="22" FontWeight="Bold" Foreground="#fdcb6e" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <ProgressBar Height="25" Value="65" Maximum="100" Foreground="#00b894"/>
                <TextBlock Text="Bir sonraki seviyeye: 350 XP" FontSize="14" Foreground="#dfe6e9" HorizontalAlignment="Center" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>
        
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <UniformGrid x:Name="AchievementsPanel" Columns="3" Margin="30,0,30,30"/>
        </ScrollViewer>
    </Grid>
</Window>
